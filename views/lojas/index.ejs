<% layout("../layout/index") -%>
<% block("title", "ON Market Place | Lojas " + nome_do_shopping_barra_titulo); -%>


     <div class="full">
    
        <aside class="bannerint">
        	<img src="/imagens/bannerloja.jpg" width="1366" height="285" title="Loja online">
            <section>Lojas</section>          
        </aside> <!--banner-->
   
   <div class="filtroloja content fgrey">
   	<form action="" method="POST" class="lojafilter"  autocomplete="off">
    	<div>
        	<label>Nome da Loja</label> 
            <input type="text" name="nomeloja" id="nomeloja" value="" placeholder="">            
        </div>
        <div>
        	<label>Tipo</label>
            <select class="minimal">
            	<option>Produtos</option>
                <option>Produtos 1</option>
                <option>Produtos 2</option>
                <option>Produtos 3</option>
            </select>
        </div>  
         <div>
        	<label>Categoria</label>
            <select class="minimal" name="categoria">
            	<option value="">Todas</option>
				<% 
				if (categorias.length>0){
				categorias.forEach(function(item) { 
				%> 
                <option value=""><%=item%></option>
				<% 
				}); 
				}
				%>
            </select>
        </div>
      <div>
       	<button>Perto de você
        <img src="/imagens/location.png" width="25" height="34">
        </button>
		<input type="hidden" name="_csrf" value="<%= csrfToken %>">	
      </div>

    </form>
   </div>     

       
 <!------------------Lojas---------------------->  

   <div id="lojas">   
   	
		<% 
		if (resultados.length>0){
		resultados.forEach(function(resultado) { 
		%>
		<div class="lojaItem" data-size="<%=resultado.fantasy_name.toLowerCase()%>">	
    	<a href="/lojas/loja/<%=resultado.slug%>" class="store" >
            <img src="/imagens/sapato.jpg" width="350" height="185"> 
            <div class="line"><%=resultado.fantasy_name%></div>
        </a>
		</div>
		<% 
		}); 
		}
		%>
        
        
   
   </div><!--produtos--> 
 </div><!--full-->


<!------------------Fim produtos---------------------->
<script type="text/javascript">
	
	
$(document).ready(function(){
	$("#nomeloja").keyup(function(e){
	  //var selectSize = $(this).val();
	  //filter(selectSize);
	  if (e.which !== 32) {
	  var value = $(this).val();
	  var noWhitespaceValue = value.replace(/\s+/g, '');
	  var noWhitespaceCount = noWhitespaceValue.length;
	  if (noWhitespaceCount > 5) {
		  filter(value.toLowerCase());
	  } else {
		$('.lojaItem').show();
	  }
	  }
	});
});
function filter(e) {
    var regex = new RegExp('\\b\\w*' + e + '\\w*\\b');
            $('.lojaItem').hide().filter(function () {
        return regex.test($(this).data('size'))
    }).show();
}

</script>